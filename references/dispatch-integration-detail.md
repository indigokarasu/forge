# ocas-forge Dispatch / Cron Integration — verbose detail

Extracted from `SKILL.md` ## Dispatch / Cron Integration to keep it under the 449-line D3 cap. The canonical decision procedure lives in `references/dispatch-pipeline-guide.md`. Key operational gotchas (full bodies):

- **SAFE PHANTOM REMOVAL (corrected 2026-07-14):** The `expanduser('~')` doubling produces a node at `<hermes-home>/profiles/indigo/home/.hermes/profiles/indigo/` — NOT a stray `home/` tree. `home/` is the cron process's actual `$HOME` and legitimately holds caches (`.bun/`, `.google_workspace_mcp/`); `rm -rf` on the whole `home/` tree breaks bun and the Google Workspace MCP. Inspect first: `find <hermes-home>/profiles/indigo/home -maxdepth 3`. Remove ONLY the doubled node: `rm -rf <hermes-home>/profiles/indigo/home/.hermes`. NEVER `rm -rf <hermes-home>/profiles/indigo/home`. Before removing, confirm the phantom node's files already exist under the real path so no unique data is lost.
- **Canonical `new_journals` executor:** `scripts/run_dispatch_pipeline.py` is broken (argparse `nargs='[]'` ValueError on Python 3.14) AND does not satisfy the explicit-run override. For any explicit-run override, run `scripts/bridge_explicit_run.py --new-files <relpaths>` — it implements the verified caller-side bridge end-to-end. **COMBINED-WAVE GAP (2026-07-15):** if the dispatch ALSO bundles `new_emails`/`taste_new_data`, the script initializes `email_triage` to an empty `indigo_inbox` with `threads_reviewed: 0` and does NOT process them. Fix: after the script runs, inspect the wave journal; if `new_emails`/`taste` was in the dispatch, handle per ocas-dispatch rules (run `verify_evidence_threads.py`; classify; do NOT draft from owner account in cron) and REWRITE the wave journal's `email_triage` + `dispatch_items` blocks to record the real account, thread count, and decision before closeout.
- **`bridge_explicit_run.py` WRONG for prior-wave-misclassification RECOVERY (2026-07-14):** The script mints a NEW `dispatch-wave-<TS>.json`. In a recovery (prior wave wrote `dispatch-wave-<EXISTINGTS>.json` with `state_updated:false` and no `forge-scan` in window), running it orphans the first and re-fires the dispatcher. Correct ONLY for a genuine first-time explicit-run wave. For recovery: hand-run the 3-pipeline (Forge no-op scan, real Mentor heartbeat via subprocess stdin, real Praxis ingest), write `forge-scan-<newTS>.json`, idempotently bridge all real outputs + the EXISTING `dispatch-wave-<EXISTINGTS>.json` into both eval stores, advance `last_ingest_run` past the max mtime, and REWRITE the EXISTING `dispatch-wave-<EXISTINGTS>.json` (same run_id) via `write_file` reflecting `classification: routine_no_op`, `state_updated: true`. Do NOT mint a new wave journal.
- **Mandatory recovery closeout (2026-07-14T13:41Z):** the pipeline bridge covers only detected `new_files` + genuine pipeline outputs. Sibling output journals written AFTER the dispatcher's `new_files` snapshot are NOT in the bridge list. After rewriting, you MUST run the one-sided reconcile sweep and assert zero gap before declaring done. **Closure helper scripts referenced do NOT EXIST on disk (2026-07-15):** `reconcile_dispatch_eval_today.py`, `verify_genuine_gap_profile.py`, `closure_convergence_sweep.py` are aspirational. Use `scripts/dispatch_redetection_close.py` instead (bounded `os.listdir` gap reconciliation → idempotent dual-store bridge → re-sweep to GENUINE GAP=0 → advance `last_ingest_run` → fix owner/indigo email state `verified_second_wave` → truncate `monitor_queue.jsonl`). `--new-files` guard exit-3s if any file is absent. **Monitor-queue re-population race (2026-07-15):** independent monitor cron can re-queue in the seconds BEFORE your state fix lands; the script's final `monitor_queue.jsonl` truncation breaks the re-fire loop — always run as LAST step.
- **KEY-SHAPE PITFALL (2026-07-13):** eval stores use the FULL relative path as key — dispatch eval `filename` = `ocas-mentor/2026-07-13/mentor-light-20260713T140647Z.json`, praxis eval `journal_id` = same full path. Idempotency guard MUST compare full relative path, NOT `os.path.basename()`. Comparing basenames against full-path values always yields "missing" and re-adds duplicates (1,384 duplicate lines accumulated). Build membership set from full value, dedupe on it; verify 0 duplicates after every bridge.
- **Override conflict — explicit-run second-wave (2026-07-11):** The explicit-run override REQUIRES the three pipeline output journals written even on second-wave. `run_dispatch_pipeline.py` classifies genuine vs second-wave SOLELY by praxis-eval membership; if all `--new-files` already evaluated, it takes second-wave branch and writes NOTHING. When dispatcher `prompt` contains explicit run instruction, caller MUST write the `forge-scan` + `praxis-dispatch` journals itself and run the REAL Mentor heartbeat — do NOT rely on the script's second-wave path.
- **Script insufficiency applies to BOTH branches (2026-07-13):** even the genuine branch bridges into PRAXIS eval ONLY — DISPATCH eval left untouched (re-detected next scan), and writes its OWN `mentor-light` no-op rather than running real heartbeat. Under any explicit-run override, caller must ALWAYS: (a) run real Mentor heartbeat separately (feed `new_files` via subprocess stdin), (b) bridge ALL output journals + detected files into BOTH eval files (idempotent by `journal_id`), (c) write dispatch-wave journal. The script can be skipped entirely for a controlled caller-side bridge.
- **Operational refinement (2026-07-11):** "Compose all timestamps once" is imperfect because the Mentor heartbeat owns its OWN journal timestamp. Practical resolution: run real heartbeat in a SEPARATE step, capture its actual journal filename via content-timestamp scan (read all `mentor-light-*.json` in today's dir, pick max `timestamp` field — do NOT use `ls -t`, mtimes lag ~7h12m; do NOT trust script stdout filename). Reuse captured filename. Forge-scan journal written by Forge step in a PRIOR call with its OWN timestamp — do NOT recompute fresh `datetime.now()` for it in bridge; glob date dir for actual `forge-scan-*.json` or pass filename forward. Only `praxis-dispatch` + `dispatch-wave` journals (bridge step ITSELF writes) may use freshly composed timestamp.
- **Pipeline scripts do NOT advance `ingest_state.json` (2026-07-13):** neither `praxis_ingest_run.py` nor `cron-heartbeat-light.py` writes it. Under explicit-run override you skip `run_dispatch_pipeline.py` and run heartbeat+ingest yourself — state advancement becomes caller's job. **State-advance rules:** (1) Set `last_ingest_run` strictly greater than max mtime of ALL journals the walk touches (including any post-dispatch mentor-cron heartbeat). (2) RE-SWEEP after advancing state (heartbeat can land between pre-advance sweep and write); iterate to 0 additions, THEN assert GENUINE GAP = 0. (3) Resync `journals_evaluated_count` / `last_eval_file_line` to actual eval-file line count.
- **Explicit dispatch prompt overrides no-op shortcut (2026-07-07):** when dispatcher `type` is `new_journals` and `prompt` contains EXPLICIT instruction to run pipelines, that instruction takes precedence over the genuine-no-op shortcut. Even if every `new_file` already evaluated, pipelines must execute. After running, still write pipeline output journals + apply third-wave eval mitigation — do NOT suppress them.
- **EXCEPTION — explicit-run wave that is itself a completed re-detection (2026-07-13T14:50Z):** If a PRIOR identical explicit-run wave already executed the three pipelines, wrote its `forge-scan-*`, ran real heartbeat + Praxis ingest, bridged everything except a residual one-sided gap, then CURRENT explicit-run wave is a re-detection. Detect pre-flight: (a) all `new_files` present in BOTH eval files, (b) `forge-scan-*.json` for wave's window already exists, (c) `last_ingest_run` past all `new_file` timestamps. When all three hold, DO NOT re-run — close ONLY residual one-sided eval gaps (bounded per-skill `os.listdir` walk, NOT recursive `glob`/`os.walk`), advance `last_ingest_run`, exit. A genuine dispatch can leave its two `new_files` split across the two eval files in OPPOSITE directions — bridge Step 4 per-file both-eval idempotent append handles this; re-detection shortcut's one-sided-gap list must enumerate BOTH orientations. Verify GENUINE GAP = 0 via bounded per-skill `os.listdir` (NOT recursive `glob`/`os.walk`) compared against profile-scoped dispatch + praxis eval stores.
- **EXCEPTION pre-flight false-negative — `forge-<TS>.json` naming (2026-07-14T12:21Z):** condition (b) requires `forge-scan-*.json` for window. BUT some waves emit Forge output as `forge-<DATE>T<ts>Z.json` (NO `-scan` suffix). When (b) is FALSE, do NOT immediately conclude 3-pipeline must re-run. Read prior `dispatch-wave-*.json` `outcome`/`notes`: if records real pipeline execution ("GENUINE GAP=0", outputs on disk), pipelines ALREADY ran — treat as completed re-detection, SKIP re-run. Correct action: bridge residual gaps + advance `last_ingest_run` + rewrite existing wave journal (`state_updated:true`).
- **Convergence sweep MUST be UNGATED (2026-07-15):** `verify_genuine_gap_profile.py` walks ALL today-dated journals with NO mtime filter. Any sweep gated on `mtime > cutoff` SKIPS journals missing from dispatch-eval but written BEFORE cutoff — they surface as genuine gaps. Fix: run `scripts/dispatch_redetection_close.py` (ungated) and iterate until it bridges 0; do NOT use cutoff-gated sweep. Loop: sweep → assert → repeat if >0.
- **Mentor-cron heartbeat convergence loop (2026-07-14T12:21Z):** `ocas-mentor` light heartbeat cron writes `mentor-light-*.json` ~every 5 min. Any recovery >5 min sees a NEW heartbeat land AFTER wave closed. After bridging gaps + advancing `last_ingest_run`, RE-SWEEP; if new cron journal with mtime above prior cutoff appears, bridge into BOTH eval stores + re-advance, sweep again. Iterate until sweep yields 0 gaps.
- **Self-consistency rule (2026-07-13T18:55Z):** Every explicit-run wave MUST complete Step 1 (write its own `forge-scan-*.json`) AND Step 4 (advance `last_ingest_run` past max mtime of all processed journals). If a PRIOR wave misclassifies itself (writes "No pipeline re-runs"/second-wave, skips BOTH), CURRENT wave's pre-flight (b)+(c) both FALSE even though (a) true — EXCEPTION shortcut will NOT fire, full 3-pipeline re-runs (double-writes wave journals). Fix is upstream: explicit-run wave must NEVER exit without its own `forge-scan-*.json` + advanced `last_ingest_run`. If you are CURRENT wave recovering such misclassification: prior wave likely already wrote+registered a `dispatch-wave-<TS>.json` in both eval stores — do NOT mint second wave journal, REWRITE that existing file (same Run ID) to reflect genuine pipeline (`classification: routine_no_op`, full `journal_pipeline` + `email_triage` notes, `state_updated: true`). Minting new `dispatch-wave-<NEWTS>.json` orphans old one and re-fires dispatcher.
- **RECOVERY PITFALL — `gap_backfill.py` FALSE all-clear (2026-07-14T07:58Z):** `gap_backfill` is mtime-based; ~7h12m mtime-lag means just-run genuine ingest's outputs carry mtimes far behind content timestamps. After recovery ingest, `gap_backfill` prints `Found 0 unevaluated journals (mtime > last_ingest_run)` even though outputs MISSING from DISPATCH eval store (praxis ingest populates only praxis-eval). A `0` during recovery is NOT trustworthy. Override: after genuine ingest, run full two-store on-disk reconciliation (glob `commons/journals/ocas-*/YYYY-MM-DD/*.json`, skip `dispatch-wave-*`; require every file in BOTH stores; bridge missing via manual dual-store append). Only verified GENUINE GAP = 0 across both stores closes re-fire loop.
- **REWRITE-TARGET SELECTION (2026-07-14T08:40Z):** Among waves registered in BOTH eval stores for the date, keep those whose content `timestamp` `>=` max(mtime of detected `new_files`), then pick MINIMUM such timestamp. Never mint a new wave.
- **RECOVERY REWRITE journal is dispositive proof (2026-07-14T13:45Z):** existing `dispatch-wave-<TS>.json` may be a RECOVERY REWRITE (recognizable by `rewritten_at` field, `summary` beginning `RECOVERY REWRITE…`, populated `journal_pipeline` block recording `forge.ran`/`mentor.ran`/`praxis.ran` with bridge counts + `actions_taken.state_updated: true`). This ALONE proves three pipelines executed — do NOT need to locate `forge-scan-*.json` or re-run. Two cheap confirms: (1) output journals named in `journal_pipeline` exist on disk; (2) both `new_files` present in BOTH eval stores and `last_ingest_run` (read via `json.load`, NEVER `read_file`) past their timestamps. If both → apply EXCEPTION closure, exit. Do NOT mint new wave journal. **Closure phantom-guard scoping:** scope on-disk-existence check to CURRENT-WAVE entries. A full-store walk re-flags pre-existing mis-anchored entries from earlier same-day waves — do NOT auto-fix those (unscoped historical cleanup forbidden).
- **Newer concurrent wave already closed (2026-07-14T14:59Z):** A re-detection may be redundant because a DIFFERENT LATER wave (timestamp AFTER dispatcher's `detected_at`) already fully processed same `new_files`. Detection: grep `dispatch-wave-*.json` for entries with `timestamp > detected_at` referencing same files/threads. If recorded `genuine_gap=0`/email `action:none`, current wave is doing already-done work. Do NOT re-run. Do NOT mint/rewrite wave journal. Execute closure-only: bridge residual post-wave cron gaps with manual dual-store append (key `journal_id` praxis-eval, `filename` dispatch-eval — `scripts/bridge_eval_inline.py` does NOT exist on disk, confirmed 2026-07-15; verified recipe in `references/mixed-wave-redetection-closure.md`). Mentor-cron convergence loop is CONTINUOUS — bridge then RE-SWEEP with bounded `os.listdir` loop advancing `last_ingest_run` to newest bridged mtime, iterating until sweep adds 0. INCLUDE post-wave heartbeat journal.
- **`bridge_explicit_run.py` writes wrong-account `email_triage` stub (2026-07-14):** template hardcodes `indigo_inbox` threads_reviewed:0. For mixed `new_journals`+`new_emails` wave wrong on two counts: (1) account usually `owner`; (2) email second-wave correct record is `actionable: 0, classification: "second-wave"`. After script returns `DONE`, patch wave journal's `email_triage` block to name real account + record email second-wave classification. SEPARATELY update AUTHORITATIVE email state file at account-subdirectory path `<hermes-home>/profiles/indigo/commons/data/ocas-dispatch/owner/last_email_check.json` (or `indigo/`). Do NOT write legacy flat files. Set `last_dispatch`, `last_dispatch_wave`, `last_dispatch_note`, `verified_second_wave: true`. **Stale-read guard (2026-07-14):** profile `commons` reachable both as real path and via symlink target; relative read through symlinked cwd returned STALE June-28 copy. ALWAYS address state file by FULL ABSOLUTE profile path, re-read IMMEDIATELY before writing.
- **PHANTOM `dispatch-wave` filename in `os.listdir` (2026-07-14 live recovery):** a `dispatch-wave-<detected_at_TS>.json` may appear in `os.listdir` but NOT on disk (`read_file` fails, `ls` confirms absence) — transient phantom, NOT real rewrite target. Do NOT trust `os.listdir` name. Find genuine prior-wave journal by eval-store membership: the `dispatch-wave-<TS>.json` present in BOTH `journals_evaluated.jsonl` stores whose run corresponds to misclassified wave IS the file to `write_file`-rewrite. Always `ls`/`os.path.exists` verify any wave filename surfaced only by `os.listdir`.
- **Mixed `new_journals`+`new_emails` in ONE wave (2026-07-14T21:50Z):** resolve as (1) `new_journals` = GENUINE dispatch-eval gap → run FULL explicit-run pipeline + bridge ALL outputs + sibling post-dispatch cron journals into BOTH eval stores; (2) `new_emails` = REDUNDANT re-detection → do NOT re-triage; prior wave already closed Path-B gaps in its triage evidence journal (itself a genuine dispatch-eval gap — bridge IT too); (3) re-affirm account state file `verified_second_wave:true` via full-file `write_file` rewrite WITHOUT touching inbox. Closing dispatch-eval gaps (not email re-detection) stops re-firing. Verify GENUINE GAP = 0 via bounded per-skill `os.listdir` (NOT recursive `glob`/`os.walk`). Full recipe: `references/session-20260714-dispatch-2150Z-mixed-gap-secondwave.md`.
- **Second-wave detection:** journal timestamp BEFORE dispatch `detected_at` → second-wave (write no-op). ANY `new_file` not in eval file → genuine dispatch, run full pipeline.
- **Eval file gap edge cases (2026-06-26 #142/#143/#144/#145/#146/#152/#154/#155/#159):** `last_ingest_run` coverage is never sufficient — ALWAYS `grep -q "filename" journals_evaluated.jsonl` for EVERY `new_file` individually regardless of `source` (dispatcher vs cron) or same-wave sibling presence. Cron pipeline journals (`praxis-cron-*`, `mentor-light-*`) written BETWEEN waves are routinely missing. Second-wave = add gaps + advance state, never write new journals. Post-ingest cron gap: cron continues writing after `last_ingest_run`; do one more `find` for `.json` with mtime after `last_ingest_run` NOT in eval file, append with source `post-dispatch-cleanup`, advance state. First cleanup after eval tracking begins backfills 800+ legacy files — one-time, not an error.
- **Third-wave mitigation scope / self-referential pattern (2026-06-22):** after Praxis ingest, caller must add all dispatch-output journals (especially forge-scan) to `journals_evaluated.jsonl` and advance `last_ingest_run` past their mtimes to prevent third-wave re-detection.
- **Phantom file cleanup pattern (2026-06-25/#100/#150):** f-string `{}` in `python3 -c` heredocs consumed by bash `${}` expansion → empty/corrupted filenames; double timestamps from shell variable interpolation. After every dispatch run, `ls` journal dir, check for empty/double/malformed timestamps, rename/fix. Beware false positives: timestamps in 20:xx:xx hour contain "2026" — valid.
- **Path typo `oras-praxis` vs `ocas-praxis` (2026-06-29):** canonical `<hermes-home>/profiles/indigo/commons/data/ocas-praxis/` — verify with `ls -d .../commons/data/oca*`. Also: commands placed AFTER JSON/YAML content but BEFORE `EOF` in `cat > file << EOF` are written into the file — never place commands after content; use `write_file` for JSON/YAML.
- **`execute_code` blocked in cron mode** — use `terminal()` heredoc Python instead. Confirmed 2026-06-26.
- **`skill_view`/`search_files` transient backend API error in cron (2026-07-15):** fall back to reading SKILL.md from disk (`find ... -name SKILL.md | grep <name>` then `read_file`) — pipeline fully runnable without `skill_view`. Tool fault, not permanent.
- **`write_file` line-wrapping corrupts Python (2026-06-30T10:20Z)** — silently wraps long lines (~80 chars), splitting string literals. After writing Python via `write_file`, `py_compile.compile` before executing. For >30-line scripts write to `/tmp/`, compile-check, run.
- **`run_dispatch_pipeline.py` Python 3.14 compat** — `nargs='*'` not `nargs='[]'`. Verified 2026-06-30.
- **Praxis-cron double-Z timestamp bug still active** — `ts.rstrip('Z') + 'Z'` on value already ending Z. Mitigation: dispatch treats filenames as-is. Fix needed in `praxis_ingest_run.py`/`praxis_common.py`.
- **Inline Python blocks compose own timestamps independently of prior shell `TS`** — compose ALL timestamps inside SAME Python block, or write filenames to temp file for subsequent steps to read verbatim. Never carry shell `TS` across `terminal()` boundaries.
- **Hand-written verify/closure path traps (2026-07-14):** (1) journals live under `commons/journals/<skill>/...` NOT `commons/<skill>/...`; anchor on `.../commons/journals/`. (2) Recursive `glob` descends into self-nested `journals/journals/...` symlinks → dozens false gaps; walk per-skill with bounded `os.listdir`.
- **`bridge_eval_inline.py` `--action` value leaks as path (2026-07-14T19:22Z):** parser kept token after `--action` as a relpath. Fixed 2026-07-14 by consuming value after `--action`. Safest: put `--action <label>` at END of arg list.
- **Dispatch-wave journal filename must use full `YYYYmmddTHHMMSSZ`** — derive from `TS=$(date -u +%Y%m%dT%H%M%SZ)`. Truncated timestamp breaks `run_id` match. If written in separate Python step, reuse exact `TS` from Forge/Mentor steps or read back filename verbatim.
- **Dispatcher new_file vs heartbeat output journal divergence (2026-06-29T04:40Z):** third-wave mitigation typically registers only heartbeat's output journal #2, missing dispatcher's original `new_file` #1 (distinct file, distinct timestamp). After mitigation, ALWAYS re-grep each `new_file` individually; register missing with `action_taken: "dispatch_new_journal_registration"`.
- **Cross-skill journal gap (#154):** after standard ingest, run final `find` for ANY `.json` in `commons/journals/` with mtime after `last_ingest_run` NOT in eval file (non-standard naming: `ocas-rally` `jrn_*.json`, `ocas-finch` `scan-*.json`). Do before third-wave mitigation.
- **Complete eval gap pattern catalog (7 patterns, #142–#160):** see SKILL.md table — cron gaps, tight/eval gaps, post-ingest, before-ingest, cross-skill, dispatch-output, post-dispatch cron. All resolved by per-`new_file` grep + bounded `os.listdir` reconciliation.
- **Eval file entries must use relative paths** — relative to `commons/journals/` (e.g. `ocas-forge/2026-06-26/forge-scan-TS.json`). Absolute paths break dispatcher grep checks.
- **`grep -c` in shell conditionals** produces unexpected exit codes — use `grep -q` for booleans.
- **Verification grep false negative from `$(date)` micro-differences** — use partial timestamp match (`grep "forge-scan-20260626T0927"`).
- **`ingest_state.json` in `new_files` is not a journal** — state file, always "new"; skip if already in eval.
- **Eval entry path base: `commons/journals/` not profile root** — use `os.path.relpath(fpath, "commons/journals")` not `os.path.relpath(fpath, ".")`.
